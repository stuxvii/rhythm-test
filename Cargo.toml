[package]
name = "rhythm"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = { version = "0.4.14", features = ["audio"] }

[profile.release]
panic = "abort"
opt-level = "z"
strip = "symbols"

[build]
rustflags = ["-C", "target-cpu=native"]
